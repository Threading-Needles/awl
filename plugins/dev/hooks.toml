# Claude Code Hooks for Catalyst Dev Plugin
#
# Previously contained 8 hooks for tracking thoughts documents.
# Now empty - Linear documents are tracked via ticket ID, not filesystem events.
#
# The workflow-context.sh script tracks the current ticket, and commands
# query Linear directly to find documents attached to that ticket.
